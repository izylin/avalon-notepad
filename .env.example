# GitHub fine-grained personal access token with Issues: Read and write,
# and Contents: Read and write for the repository below. Never expose this in NEXT_PUBLIC_*.
FEEDBACK_GITHUB_TOKEN=
FEEDBACK_GITHUB_REPOSITORY=izylin/avalon-notepad
# Set this if the repository's default branch is not main.
FEEDBACK_GITHUB_BRANCH=main
